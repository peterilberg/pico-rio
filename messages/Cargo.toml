[package]
name = "messages"
version = "0.1.0"
edition = "2024"

[dependencies]
heapless = { version = "0.9", features = ["serde"] }
serde = { version = "1.0.203", default-features = false, features = ["derive"] }
